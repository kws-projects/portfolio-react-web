# Deployment config (no secrets).
# Copy this file along with docker-compose.yaml to the deploy machine.
# Update VERSION when deploying a new release.
PORTFOLIO_IMAGE=kennwong/portfolio-react-web
PORTFOLIO_PORT_BINDING=3000:3000
VERSION=2.2.1
